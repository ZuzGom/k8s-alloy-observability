# Optional overrides for mcp-grafana (defaults work with make port-forward-grafana).
# Password is normally read from ~/.cursor/grafana-query.env after make setup-mcp-grafana.
GRAFANA_URL=http://127.0.0.1:3000
GRAFANA_USER=admin
GRAFANA_PASSWORD=
