apiVersion: batch/v1
kind: Job
metadata:
  name: k6-load-generator
  namespace: apps
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: k6
          image: grafana/k6:latest
          command: ["k6", "run", "/scripts/test.js"]
          env:
            - name: TARGET_URL
              value: "http://productpage.apps.svc.cluster.local:9080/productpage?product=1"
          volumeMounts:
            - name: script
              mountPath: /scripts
      volumes:
        - name: script
          configMap:
            name: k6-script