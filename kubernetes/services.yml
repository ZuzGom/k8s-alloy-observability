apiVersion: v1
kind: Service
metadata:
  name: productpage
  namespace: apps
  labels:
    app: productpage
spec:
  selector:
    app: productpage
  ports:
    - name: http
      port: 9080
      targetPort: 9080
---
apiVersion: v1
kind: Service
metadata:
  name: details
  namespace: apps
  labels:
    app: details
spec:
  selector:
    app: details
  ports:
    - name: http
      port: 9080
      targetPort: 9080
---
apiVersion: v1
kind: Service
metadata:
  name: ratings
  namespace: apps
  labels:
    app: ratings
spec:
  selector:
    app: ratings
  ports:
    - name: http
      port: 9080
      targetPort: 9080
---
apiVersion: v1
kind: Service
metadata:
  name: reviews
  namespace: apps
  labels:
    app: reviews
spec:
  selector:
    app: reviews
  ports:
    - name: http
      port: 9080
      targetPort: 9080
